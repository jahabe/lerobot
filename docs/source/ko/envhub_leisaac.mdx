# LeIsaac × LeRobot EnvHub

LeRobot EnvHub가 이제 LeIsaac을 통한 **시뮬레이션 내 모방 학습**을 지원합니다. 일상적인 조작 작업을 실행하고, 로봇을 텔레오퍼레이션하여 데모 데이터를 수집하고, Hub에 업로드한 뒤 LeRobot에서 정책을 학습하는 전 과정을 하나의 루프로 진행할 수 있습니다.

[LeIsaac](https://github.com/LightwheelAI/leisaac)은 IsaacLab 및 SO101 리더/팔로워 구성과 통합되어 다음을 제공합니다:

- 🕹️ 데이터 수집의 초적화를 위한 **텔레오퍼레이션 우선 워크플로우**
- 📦 LeRobot 학습에 바로 사용 가능한 **내장 데이터 변환**
- 🤖 오렌지 잡기, 큐브 들어올리기, 테이블 정리, 옷 접기 등 **일상적인 스킬**
- ☁️ 클라우드 시뮬레이션, EnvHub 지원, Sim2Real 도구 등 [LightWheel](https://lightwheel.ai/)의 **지속적인 업그레이드**

아래에서 LeRobot EnvHub를 통해 제공되는 LeIsaac 작업 목록을 확인할 수 있습니다. 

# 사용 가능한 환경

아래 표는 LeIsaac x LeRobot EnvHub에서 사용 가능한 모든 태스크와 환경을 보여줍니다. 다음 명령어를 실행하면 최신 환경 목록을 확인할 수 있습니다:

```bash
python scripts/environments/list_envs.py
```

| 태스크                                                                                                                                                            | 환경 ID                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 태스크 설명                                                                                                           | 관련 로봇                                              |
| :-------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------- | :--------------------------------------------------------- |
| <video src="https://github.com/user-attachments/assets/466eddff-f720-4f99-94d5-5e123e4c302c" autoplay loop muted playsinline style="max-width: 300px;"></video> | [LeIsaac-SO101-PickOrange-v0](https://github.com/LightwheelAI/leisaac/blob/main/source/leisaac/leisaac/tasks/pick_orange/pick_orange_env_cfg.py)<br /><br />[LeIsaac-SO101-PickOrange-Direct-v0](https://github.com/LightwheelAI/leisaac/blob/main/source/leisaac/leisaac/tasks/pick_orange/direct/pick_orange_env.py)                                                                                                                                                                                                                        | 오렌지 세 개를 집어 접시에 담은 다음, 팔을 초기 상태로 되돌립니다.                                          | Single-Arm SO101 Follower                                  |
| <video src="https://github.com/user-attachments/assets/1e4eb83a-0b38-40fb-a0b2-ddb0fe201e6d" autoplay loop muted playsinline style="max-width: 300px;"></video> | [LeIsaac-SO101-LiftCube-v0](https://github.com/LightwheelAI/leisaac/blob/main/source/leisaac/leisaac/tasks/lift_cube/lift_cube_env_cfg.py)<br /><br />[LeIsaac-SO101-LiftCube-Direct-v0](https://github.com/LightwheelAI/leisaac/blob/main/source/leisaac/leisaac/tasks/lift_cube/direct/lift_cube_env.py)                                                                                                                                                                                                                                    | 빨간 큐브를 들어올립니다.                                                                                                      | Single-Arm SO101 Follower                                  |
| <video src="https://github.com/user-attachments/assets/e49d8f1c-dcc9-412b-a88f-100680d8a45b" autoplay loop muted playsinline style="max-width: 300px;"></video> | [LeIsaac-SO101-CleanToyTable-v0](https://github.com/LightwheelAI/leisaac/blob/main/source/leisaac/leisaac/tasks/clean_toy_table/clean_toy_table_env_cfg.py)<br /><br />[LeIsaac-SO101-CleanToyTable-BiArm-v0](https://github.com/LightwheelAI/leisaac/blob/main/source/leisaac/leisaac/tasks/clean_toy_table/clean_toy_table_bi_arm_env_cfg.py)<br /><br />[LeIsaac-SO101-CleanToyTable-BiArm-Direct-v0](https://github.com/LightwheelAI/leisaac/blob/main/source/leisaac/leisaac/tasks/clean_toy_table/direct/clean_toy_table_bi_arm_env.py) | 알파벳 'e' 모양 물체 두 개를 박스에 넣고, 팔을 초기 상태로 되돌립니다.                                                    | Single-Arm SO101 Follower<br /><br />Bi-Arm SO101 Follower |
| <video src="https://github.com/user-attachments/assets/e29a0f8a-9286-4ce6-b45d-342c3d3ba754" autoplay loop muted playsinline style="max-width: 300px;"></video> | [LeIsaac-SO101-FoldCloth-BiArm-v0](https://github.com/LightwheelAI/leisaac/blob/main/source/leisaac/leisaac/tasks/fold_cloth/fold_cloth_bi_arm_env_cfg.py)<br /><br />[LeIsaac-SO101-FoldCloth-BiArm-Direct-v0](https://github.com/LightwheelAI/leisaac/blob/main/source/leisaac/leisaac/tasks/fold_cloth/direct/fold_cloth_bi_arm_env.py)                                                                                                                                                                                                    | 옷을 접고, 팔을 초기 상태로 되돌립니다.<br /><br />_참고: 이 태스크에서 check_success는 DirectEnv에서만 지원됩니다._ | Bi-Arm SO101 Follower                                      |

# 코드 한 줄로 LeRobot에서 LeIsaac 바로 사용하기

> EnvHub: HuggingFace를 통해 LeIsaac 환경 공유하기

[EnvHub](https://huggingface.co/docs/lerobot/envhub)는 재현 가능한 환경 허브입니다. 코드 한 줄로 패키지된 시뮬레이션을 실행하고, 즉시 실험하며, 커뮤니티에 직접 태스크를 게시할 수 있습니다.

LeIsaac은 EnvHub를 지원하므로 몇 가지 명령어만으로 태스크를 사용하거나 공유할 수 있습니다.

<video
  controls
  src="https://github.com/user-attachments/assets/687666f5-ebe0-421d-84a0-eb86116ac5f8"
  style={{ width: "100%", maxWidth: "960px", borderRadius: "8px" }}
/>

## 환경 설정 시작하기

아래 명령어를 실행하여 코드 환경을 설정합니다:

```bash
# leisaac 설치는 시작하기/설치 가이드를 먼저 참고하세요
conda create -n leisaac_envhub python=3.11
conda activate leisaac_envhub

conda install -c "nvidia/label/cuda-12.8.1" cuda-toolkit
pip install -U torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu128
pip install 'leisaac[isaaclab] @ git+https://github.com/LightwheelAI/leisaac.git#subdirectory=source/leisaac' --extra-index-url https://pypi.nvidia.com

# lerobot 설치
pip install lerobot==0.4.1

# numpy 버전 고정
pip install numpy==1.26.0
```

## 사용 예시

EnvHub는 LeIsaac에서 지원하는 모든 태스크를 동일한 인터페이스로 제공합니다. 아래 예시는 `so101_pick_orange`를 불러와 랜덤 액션 롤아웃과 인터랙티브 텔레오퍼레이션을 수행합니다.

### 랜덤 액션

<details>
<summary>코드 예시 펼치기</summary>

```python
# envhub_random_action.py

import torch
from lerobot.envs import make_env

# Hub에서 환경 불러오기
envs_dict = make_env("LightwheelAI/leisaac_env:envs/so101_pick_orange.py", n_envs=1, trust_remote_code=True)

# 환경 접근
suite_name = next(iter(envs_dict))
sync_vector_env = envs_dict[suite_name][0]
# retrieve the isaac environment from the sync vector env
env = sync_vector_env.envs[0].unwrapped

# 일반 gym 환경처럼 사용
obs, info = env.reset()

while True:
    action = torch.tensor(env.action_space.sample())
    obs, reward, terminated, truncated, info = env.step(action)
    if terminated or truncated:
        obs, info = env.reset()

env.close()
```

</details>

```bash
python envhub_random_action.py
```

스크립트를 실행하면 SO101 암이 완전한 랜덤 명령으로 움직이는 것을 확인할 수 있습니다.

### 텔레오퍼레이션

LeRobot의 텔레오퍼레이션 스택으로 시뮬레이션 암을 조작할 수 있습니다.

SO101 리더 컨트롤러를 연결하고 아래 캘리브레이션 명령어를 실행합니다.

```bash
lerobot-calibrate \
    --teleop.type=so101_leader \
    --teleop.port=/dev/ttyACM0 \
    --teleop.id=leader
```

이후 텔레오퍼레이션 스크립트를 실행합니다.

<details>
<summary>코드 예시 펼치기</summary>

```python
# envhub_teleop_example.py

import logging
import time
import gymnasium as gym

from dataclasses import asdict, dataclass
from pprint import pformat

from lerobot.teleoperators import (  # noqa: F401
    Teleoperator,
    TeleoperatorConfig,
    make_teleoperator_from_config,
    so_leader,
    bi_so_leader,
)
from lerobot.utils.robot_utils import precise_sleep
from lerobot.utils.utils import init_logging
from lerobot.envs import make_env


@dataclass
class TeleoperateConfig:
    teleop: TeleoperatorConfig
    env_name: str = "so101_pick_orange"
    fps: int = 60


@dataclass
class EnvWrap:
    env: gym.Env


def make_env_from_leisaac(env_name: str = "so101_pick_orange"):
    envs_dict = make_env(
        f'LightwheelAI/leisaac_env:envs/{env_name}.py',
        n_envs=1,
        trust_remote_code=True
    )
    suite_name = next(iter(envs_dict))
    sync_vector_env = envs_dict[suite_name][0]
    env = sync_vector_env.envs[0].unwrapped

    return env


def teleop_loop(teleop: Teleoperator, env: gym.Env, fps: int):
    from leisaac.devices.action_process import preprocess_device_action
    from leisaac.assets.robots.lerobot import SO101_FOLLOWER_MOTOR_LIMITS
    from leisaac.utils.env_utils import dynamic_reset_gripper_effort_limit_sim

    env_wrap = EnvWrap(env=env)

    obs, info = env.reset()
    while True:
        loop_start = time.perf_counter()
        if env.cfg.dynamic_reset_gripper_effort_limit:
            dynamic_reset_gripper_effort_limit_sim(env, 'so101leader')

        raw_action = teleop.get_action()
        processed_action = preprocess_device_action(
            dict(
                so101_leader=True,
                joint_state={
                    k.removesuffix(".pos"): v for k, v in raw_action.items()},
                motor_limits=SO101_FOLLOWER_MOTOR_LIMITS),
            env_wrap
        )
        obs, reward, terminated, truncated, info = env.step(processed_action)
        if terminated or truncated:
            obs, info = env.reset()

        dt_s = time.perf_counter() - loop_start
        precise_sleep(max(1 / fps - dt_s, 0.0))
        loop_s = time.perf_counter() - loop_start
        print(f"\ntime: {loop_s * 1e3:.2f}ms ({1 / loop_s:.0f} Hz)")


def teleoperate(cfg: TeleoperateConfig):
    init_logging()
    logging.info(pformat(asdict(cfg)))

    teleop = make_teleoperator_from_config(cfg.teleop)
    env = make_env_from_leisaac(cfg.env_name)

    teleop.connect()
    if hasattr(env, 'initialize'):
        env.initialize()
    try:
        teleop_loop(teleop=teleop, env=env, fps=cfg.fps)
    except KeyboardInterrupt:
        pass
    finally:
        teleop.disconnect()
        env.close()


def main():
    teleoperate(TeleoperateConfig(
        teleop=so_leader.SO101LeaderConfig(
            port="/dev/ttyACM0",
            id='leader',
            use_degrees=False,
        ),
        env_name="so101_pick_orange",
        fps=60,
    ))


if __name__ == "__main__":
    main()

```

</details>

```bash
python envhub_teleop_example.py
```

스크립트를 실행하면 물리적 리더 장치로 시뮬레이션 암을 직접 조작할 수 있습니다.

## ☁️ 클라우드 시뮬레이션 (GPU 불필요)

로컬 GPU나 적합한 드라이버가 없어도 괜찮습니다! LeIsaac은 별도 설정 없이 클라우드에서 완전히 실행할 수 있습니다.
LeIsaac은 **NVIDIA Brev**에서 바로 동작하며, 브라우저에서 완전히 구성된 환경을 제공합니다.

👉 **시작하기:** [https://lightwheelai.github.io/leisaac/docs/cloud_simulation/nvidia_brev](https://lightwheelai.github.io/leisaac/docs/cloud_simulation/nvidia_brev)

인스턴스가 배포되면 **포트 80 (HTTP)** 링크를 열어 **비주얼 스튜디오 코드 서버**를 실행합니다 (기본 비밀번호: `password`). 이후 웹 브라우저에서 시뮬레이션 실행, 코드 편집, IsaacLab 환경 시각화를 모두 수행할 수 있습니다.

**GPU도, 드라이버도, 로컬 설치도 필요 없습니다. 클릭 한 번으로 바로 실행됩니다.**

## 추가 참고사항

EnvHub는 LeIsaac 태스크와 동기화하여 지원을 유지합니다. 현재 지원되는 태스크:

- `so101_pick_orange`
- `so101_lift_cube`
- `so101_clean_toytable`
- `bi_so101_fold_cloth`

`make_env` 호출 시 다른 스크립트를 지정하면 태스크를 전환할 수 있습니다:

```python
envs_dict_pick_orange = make_env("LightwheelAI/leisaac_env:envs/so101_pick_orange.py", n_envs=1, trust_remote_code=True)
envs_dict_lift_cube = make_env("LightwheelAI/leisaac_env:envs/so101_lift_cube.py", n_envs=1, trust_remote_code=True)
envs_dict_clean_toytable = make_env("LightwheelAI/leisaac_env:envs/so101_clean_toytable.py", n_envs=1, trust_remote_code=True)
envs_dict_fold_cloth = make_env("LightwheelAI/leisaac_env:envs/bi_so101_fold_cloth.py", n_envs=1, trust_remote_code=True)
```

참고: `bi_so101_fold_cloth` 사용 시, 환경을 가져온 직후 다른 작업을 수행하기 전에 반드시 `initialize()`를 먼저 호출해야 합니다:

<details>
<summary>코드 예시 펼치기</summary>

```python
import torch
from lerobot.envs import make_env

# Hub에서 환경 불러오기
envs_dict = make_env("LightwheelAI/leisaac_env:envs/bi_so101_fold_cloth.py", n_envs=1, trust_remote_code=True)

# 환경 접근
suite_name = next(iter(envs_dict))
sync_vector_env = envs_dict[suite_name][0]
# retrieve the isaac environment from the sync vector env
env = sync_vector_env.envs[0].unwrapped

# 참고: initialize()를 먼저 호출해야 합니다
env.initialize()

# env 관련 작업 수행...
```

</details>
